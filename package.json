{
  "name": "ionic-stencil-shared-transition",
  "private": true,
  "version": "0.0.1",
  "description": "ionic-stencil-shared-transition",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "^4.7.1"
  },
  "devDependencies": {
    "@stencil/core": "^1.2.1"
  }
}
